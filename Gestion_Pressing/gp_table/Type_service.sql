--------Création de la table Type_service ------
Create table TYPE_SERVICE (
    id_type int(11) PRIMARY KEY NOT NULL,
    nom VARCHAR2 (30) NOT NULL,
    description_TypeService VARCHAR2 (50) NOT NULL

);